# Dépendances — installation rapide sans build du package.
# Équivalent au [project].dependencies de pyproject.toml (Phase 0).
# Les dépendances des phases ultérieures sont commentées : décommenter au besoin.

# --- Phase 0 : transcription GPU + config ---
faster-whisper>=1.1.0
nvidia-cublas-cu12
nvidia-cudnn-cu12
tomli-w>=1.0.0

# --- Phase 1 : capture micro + injection presse-papiers ---
sounddevice>=0.4.6
numpy>=1.24
pyperclip>=1.8.2

# --- Phase 2 : raccourci global ---
pynput>=1.7.6

# --- Phase 4 : systray (icône d'état + menu + notifications toast) ---
pystray>=0.19
Pillow>=10.0

# --- Phase 5 : pilule flottante always-on-top (variante D) ---
PySide6>=6.6

# --- Phase 8 : téléchargement du modèle au 1er lancement (retour visuel) ---
# Dépendances transitives de faster-whisper, épinglées car utilisées directement
# par voix_clavier.models (téléchargement + progression).
huggingface_hub>=0.20
tqdm>=4.65

# Packaging / tests (dev) : pip install pyinstaller pytest
# (ou : pip install -e .[dev])
